; Local PHP settings for `php artisan serve` / CLI.
; Prefer: ./bin/serve   (uses .php-ini/99-uploads.ini via PHP_INI_SCAN_DIR)
; Or:     /usr/bin/php8.4 -c php-server.ini artisan serve

upload_max_filesize = 20M
post_max_size = 64M
max_file_uploads = 20
memory_limit = 256M
